import React from "react";

const Dashboard = () => {
  return (
    <div style={{alignItems:'center',marginLeft:'120px'}}>
      Dashboard Screen
    </div>
  );
};

export default Dashboard;